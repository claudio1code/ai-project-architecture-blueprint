# ADR-NNN: {Decision title}

- **Status:** `Proposed / Accepted / Rejected / Deprecated / Superseded by ADR-NNN`
- **Date:** `YYYY-MM-DD`
- **Decision owners:** `{people/roles}`
- **Review date or trigger:** `{date or measurable condition}`

## Context

`{Relevant business and technical setting. Link to project context, requirements, constraints, incidents, or measurements rather than copying them.}`

## Problem

`{The specific decision to make and why it is needed now.}`

## Decision forces

- `{requirement, constraint, risk, team capability, or quality attribute}`
- `{state relative priority or non-negotiable threshold}`

## Alternatives considered

### Option A — {name}

- **How it works:** `{...}`
- **Benefits:** `{...}`
- **Costs and risks:** `{...}`
- **Evidence:** `{prototype, benchmark, prior system, documentation}`

### Option B — {name}

- **How it works:** `{...}`
- **Benefits:** `{...}`
- **Costs and risks:** `{...}`
- **Evidence:** `{...}`

Include “do nothing” or the simpler current approach when it is a real option.

## Decision

`{State the chosen option, scope, and important implementation constraints. Use present tense: “We will…”.}`

## Positive consequences

- `{benefit tied to a decision force}`

## Negative consequences

- `{complexity, limitation, cost, or lost option that is knowingly accepted}`

## Risks and mitigations

| Risk | Early signal | Mitigation/guardrail | Owner |
| --- | --- | --- | --- |
| `{risk}` | `{observable signal}` | `{action}` | `{owner}` |

## Adoption and rollback

- **Incremental adoption:** `{smallest safe step or vertical slice}`
- **Compatibility/migration:** `{consumer/data/deployment plan}`
- **Rollback or exit:** `{how to reverse or replace; irreversible parts}`
- **Validation:** `{tests, measures, review}`

## Review plan

Revisit when `{date or signal}`. At review, evaluate `{metrics, incidents, cost, assumptions}`. The decision does not change until this ADR is superseded or amended through an explicit record.

## References

- `{relative project document or authoritative external reference}`
